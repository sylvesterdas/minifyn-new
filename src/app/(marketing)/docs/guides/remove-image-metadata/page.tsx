import type { Metadata } from 'next'; import { SeoGuide } from '@/components/seo-guide';
export const metadata: Metadata = { title: 'How to Remove GPS and EXIF Data from Images | MiniFyn', description: 'Understand image metadata and remove location information before sharing photos.', alternates: { canonical: 'https://www.minifyn.com/docs/guides/remove-image-metadata' } };
export default function Page() { return <SeoGuide title="How to Remove GPS and EXIF Data from Images" description="Photos can contain hidden camera and location metadata. Remove it before publishing or sending them." steps={['Choose the image you plan to share.', 'Export a sanitized copy with EXIF and GPS stripping enabled.', 'Check the exported file rather than relying on the original.', 'Share only the sanitized copy.']} cta="CensorFyn strips metadata on-device." href="/censorfyn" />; }
