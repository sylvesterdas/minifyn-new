import Link from 'next/link';
export function SeoGuide({ title, description, steps, cta, href }: { title: string; description: string; steps: string[]; cta: string; href: string }) { return <main className="container mx-auto max-w-3xl px-4 py-12 md:py-24"><h1 className="text-4xl font-bold tracking-tight sm:text-5xl">{title}</h1><p className="mt-4 text-lg text-muted-foreground">{description}</p><ol className="mt-10 list-decimal space-y-4 pl-6 text-lg">{steps.map((step) => <li key={step}>{step}</li>)}</ol><div className="mt-12 rounded-xl border bg-card p-6"><p className="font-semibold">{cta}</p><Link className="mt-3 inline-block text-primary hover:underline" href={href}>Try MiniFyn →</Link></div></main>; }
